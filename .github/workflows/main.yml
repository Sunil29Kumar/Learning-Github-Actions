name: Test
on: push
jobs:
  my-first-job:
    runs-on: ubuntu-latest
    steps:
        - name: list all lfiles
          run: ls -a
        - name: print workig directory
          run: pwd
        - name: checkout code
          uses: actions/checkout@v4
        - name: list all files
          run: ls -a
        - name: print working file contents
          run: cat ./github/workflows/test.yml
